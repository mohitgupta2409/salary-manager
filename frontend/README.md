# Salary Manager — Frontend

React + TypeScript + Vite + Tailwind CSS application.

## Development

```bash
npm install
npm run dev
```

Runs on http://localhost:5173 with API calls proxied to http://localhost:8080.

## Build

```bash
npm run build
```

Output in `dist/` — static files deployable to any CDN or static server.
